# Overview

乒乓buffer：

* 只分配两个buffer，两块进行复用，不新建，直接原地覆盖
* 生产者写一块，消费者读另一块，每轮交换
* 这样可以将每帧耗时降低到max(生产者耗时， 消费者耗时)

工作节奏：

* 相位0：生产者写buffer[0]
* 相位1：生产者写buffer[1]，消费者读取buffer[0]
* 相位2：生产者写buffer[0]，消费者读取buffer[1]
* ...
